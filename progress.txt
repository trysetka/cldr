## Progress Log — Dynamic Runtime Locale Loading

### 2026-03-25 — Phase 1 Complete

**Beads task:** cldr-5h3: Phase 1: Cldr.Locale.RuntimeStore Core
**Completed:** RuntimeStore module with ETS + persistent_term tiered cache
**Phase reference:** PLAN.md §Phase 1

**Key decisions:**
- Used `Loader.read_locale_file!/1` + manual transform (not `Loader.do_get_locale/3`) to avoid compile-time cache interference
- Added separate `@index_table_name :cldr_runtime_locale_index` to track loaded locales regardless of migration state — fixes `known_loaded_locales/1` after migration
- `:ets.insert_new/2` serves as the mutex — winner loads, losers poll with 10ms intervals
- Migration is batched through GenServer with `Process.send_after/3` (0ms delay)
- Persistent_term key: `{Cldr.Locale.RuntimeStore, backend, locale_name}`
- All error paths return tagged tuples `{:error, reason}`

**Files changed:**
- `lib/cldr/locale/runtime_store.ex` — new module (396 lines)
- `test/cldr/locale/runtime_store_test.exs` — new test file (15 tests)

**Tests:** `mix test test/cldr/locale/runtime_store_test.exs --trace` — 15/15 pass
**Full suite:** `mix test --trace` — 13036/13036 pass, 0 failures

**Commit:** 1a349639c
**Next steps:** Phase 2 — Backend integration for locale validation. Add `do_validate_locale/1` fallback in `lib/cldr/backend/cldr_backend.ex` that checks RuntimeStore before full parsing.

---

### 2026-03-25 — Phase 2 Complete

**Beads task:** cldr-z1l: Phase 2: Backend — Locale Validation
**Completed:** RuntimeStore fallback for locale validation, known_locale_names, known_locale_name?
**Phase reference:** PLAN.md §Phase 2

**Key decisions:**
- Added `do_validate_locale/1` fallback that checks RuntimeStore before `Cldr.Locale.new/2` full parse path
- The fallback handles both string and atom inputs via separate guard clauses (`is_atom`/`is_binary`), delegates to shared `do_validate_locale_for_string/2` helper
- String locale names are converted to atoms for RuntimeStore lookup (`String.to_atom/1`) — safe because loaded locale atoms already exist
- LanguageTag is constructed via `Cldr.Config.language_tag/1` + `Cldr.Locale.put_gettext_locale_name/2` + `Map.put(:backend, ...)` — mirrors compiled path
- `@cldr_config` module attribute stores the compile-time config struct for use in `defp` (bind_quoted `config` variable has context issues in function defs)
- `known_locale_names/0` extended to merge `@known_locale_names` with `RuntimeStore.known_loaded_locales(__MODULE__)`
- `known_locale_name?/1` automatically works via its delegation to the extended `known_locale_names/0`
- Used `NoFallback.Cldr` (locales: ["en", "es-US"]) for integration tests to exercise the fallback path

**Files changed:**
- `lib/cldr/backend/cldr_backend.ex` — modified: added `@cldr_config` attribute, `do_validate_locale/1` RuntimeStore fallback, extended `known_locale_names/0`
- `test/cldr/locale/runtime_store_integration_test.exs` — new test file (7 tests)

**Tests:** `mix test test/cldr/locale/runtime_store_integration_test.exs --trace` — 7/7 pass
**Full suite:** `mix test --trace` — 13043/13043 pass, 0 failures

**Commit:** c06b705c0
**Next steps:** Phase 3 — Backend integration for locale data accessors (quote_marks_for, ellipsis_chars_for, lenient_parse_map, script_direction_from_locale)

---

### 2026-03-25 — Phase 3 Complete

**Beads task:** cldr-cud: Phase 3: Backend — Locale Data Accessors
**Completed:** RuntimeStore fallbacks for all locale data accessor functions
**Phase reference:** PLAN.md §Phase 3

**Key decisions:**
- Added `quote_marks_for/1` fallback that reads `:delimiters` from RuntimeStore data
- Added `ellipsis_chars_for/1` fallback that reads `:ellipsis` from RuntimeStore data
- Added `lenient_parse_map/2` fallback that applies same regex transformation as compile-time (remove compounds, escape fixes, atomize keys)
- Added `script_direction_from_locale/1` fallback in locale.ex that reads `:layout.character_order` — this also fixes a pre-existing infinite recursion bug where the catch-all would loop through validate_locale → LanguageTag → catch-all
- All fallbacks use `RuntimeStore.fetch_locale/2` as the single data source
- Pre-configured locales retain 0ns compiled function clause fast path (zero regression)

**Files changed:**
- `lib/cldr/backend/cldr_backend.ex` — added 3 fallback clauses (quote_marks_for, ellipsis_chars_for, lenient_parse_map)
- `lib/cldr/backend/locale.ex` — added script_direction_from_locale fallback before existing catch-all
- `test/cldr/locale/runtime_store_integration_test.exs` — extended with 8 new tests (15 total)
- `PLAN.md` — updated Phase 3 checkboxes and quality gate

**Tests:** `mix test test/cldr/locale/runtime_store_integration_test.exs --trace` — 15/15 pass
**Full suite:** `mix test --trace` — 13051/13051 pass, 0 failures

**Commit:** 4727d6275
**Next steps:** Phase 4 — Public API (Cldr.load_locale, Cldr.unload_locale, Cldr.loaded_locale_names, Cldr.locale_loaded? + backend shorthands)

---

### 2026-03-25 — Phase 4 Complete

**Beads task:** cldr-olq: Phase 4: Public API
**Completed:** Clean public API for runtime locale loading on Cldr and backend modules
**Phase reference:** PLAN.md §Phase 4

**Key decisions:**
- `Cldr.load_locale/2` delegates to `RuntimeStore.load_locale/2`, converts `{:ok, data}` → `:ok` for clean API
- `Cldr.unload_locale/2`, `Cldr.loaded_locale_names/1`, `Cldr.locale_loaded?/2` pass through directly to RuntimeStore
- Backend shorthand functions (`Backend.load_locale/1`, etc.) delegate to `Cldr.*` with `__MODULE__` as backend
- `load_locale/2` accepts both atom and string locale names (converts string to atom)
- All functions have `@spec`, `@doc` with examples in iex format
- Doctests use load-then-unload patterns to avoid state leakage between async doctests

**Files changed:**
- `lib/cldr.ex` — added 4 public API functions (~120 lines including docs)
- `lib/cldr/backend/cldr_backend.ex` — added 4 backend shorthand functions with docs
- `test/cldr/locale/runtime_store_api_test.exs` — new test file (14 tests)
- `PLAN.md` — updated Phase 4 checkboxes and quality gate

**Tests:** `mix test test/cldr/locale/runtime_store_api_test.exs --trace` — 14/14 pass
**Full suite:** `mix test --trace` — 13065/13065 pass, 0 failures

**Commit:** 59fa4b589
**Next steps:** Phase 5 — Provider Integration (add catch-all fallback clauses in external provider packages)

---

### 2026-03-25 — Phase 5 (in-repo work) Complete

**Beads task:** cldr-ll8: Phase 5: Provider Integration
**Completed:** Data contract verification tests + critical transform pipeline bug fix
**Phase reference:** PLAN.md §Phase 5

**Key decisions:**
- Provider packages are external — cannot modify in this repo. In-repo work focused on verifying the data contract.
- **Critical bug found and fixed:** `transform_locale_data/2` had wrong operation order — `atomize_keys(level: 1..1)` ran BEFORE `integerize_keys`, causing string keys like `"2"` to become atoms (`:"2"`) instead of remaining as strings for `integerize_keys` to convert to integers (`2`). Reordered to match `Loader.do_get_locale/3` exactly.
- Added 27 data contract tests proving RuntimeStore returns Loader-identical data for all provider keys (number_formats, list_formats, dates, territories, units, person_names, etc.)
- Verified provider fallback pattern: `fetch_locale/2` returns `{:ok, data}` with correct keys for each provider's extraction pattern

**Files changed:**
- `lib/cldr/locale/runtime_store.ex` — reordered `transform_locale_data/2` pipeline
- `test/cldr/locale/runtime_store_contract_test.exs` — new test file (27 tests)
- `PLAN.md` — updated Phase 5 steps, tests, quality gate

**Tests:** `mix test test/cldr/locale/ --trace` — 71/71 pass
**Full suite:** `mix test --trace` — 13092/13092 pass, 0 failures

**Commit:** cae4ea3fd
**Next steps:** Phase 5 provider PRs (external packages). Each provider needs catch-all clauses delegating to `RuntimeStore.fetch_locale/2`. Remaining Phase 5 steps: per-provider fallback clauses in ex_cldr_numbers, ex_cldr_dates_times, ex_cldr_units, ex_cldr_lists, ex_cldr_territories, ex_cldr_calendars, ex_cldr_person_names.

---

### 2026-03-25 — Phase 5 Provider Catch-Alls Implemented

**Completed:** Provider catch-all clauses for 4 providers + shared helper
**Phase reference:** PLAN.md §Phase 5

**Key decisions:**
- Added `Backend.fetch_locale_data(key, locale_name)` helper in core — providers use this instead of calling RuntimeStore directly
- All provider catch-all clauses use `when is_atom(locale_name)` guard to match BEFORE the existing `validate_locale` → re-dispatch fallback (which infinite-loops for dynamic locales)
- Architecture B providers (cldr_dates_times, cldr_calendars, cldr_person_names) already have catch-alls — no changes needed
- Provider repos cloned as subdirectories (own .git) — changes committed there separately

**Provider changes:**
- cldr_lists: `list_patterns_for/1`, `list_formats_for/1` — fetch `:list_formats`, struct-transform
- cldr_numbers/symbol: `number_symbols_for/1` — fetch `:number_symbols`, struct-transform
- cldr_numbers/format: `all_formats_for/1`, `minimum_grouping_digits_for/1`, `default_grouping_for/1`
- cldr_territories: 6 one-arity + 2 three-arity catch-alls — fetch `:territories`/`:subdivisions`
- cldr_units: `units_for/2` — fetch `:units`, style-filtered transform

**Files changed (core):**
- `lib/cldr/backend/cldr_backend.ex` — `fetch_locale_data/2` helper
- `test/cldr/locale/runtime_store_contract_test.exs` — 4 new helper tests

**Commits (core):** 342f41c86, cae4ea3fd
**Tests:** 13096/13096 pass, 0 failures

**Remaining for Phase 5:** Provider changes need to be committed in their own repos and tested individually. cldr_dates_times, cldr_calendars, cldr_person_names don't need changes (Architecture B).
